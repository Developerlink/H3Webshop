import React from 'react';

const ProductForm = (props) => {


    return (
      <div>
        <h1>This displays a product for creation or modification</h1>

        <button className="btn btn-primary" >Increment</button>
      </div>
    );
  
};

export default ProductForm;
